$(".v-click").on("click", function(){

    switch ($(this).data('accion')) {

      case valor1:
        // Código a ejecutar cuando la expresión coincide con valor1
        break;

      case valor2:
        // Código a ejecutar cuando la expresión coincide con valor2
        break;

      //default:
        // Código a ejecutar si ninguno de los casos coincide con la expresión

    }

});



